# Generated by ReXGlue v0.8.0.0-dev.unknown on 2026-06-13 20:15:23 UTC.

[project]
name = "metalslugxx"
sdk_version = "0.8.0.0"
game_root = "../Assets/58410972"

[entrypoint]
file_path = "../Assets/58410972/default.xex"
out_directory_path = "generated/default"
includes = [ "metalslugxx_config.toml", "metalslugxx_vtables.toml", "metalslugxx_callbacks.toml" ]

# CRT setjmp/longjmp — located 2026-06-13 by disassembling the decrypted
# basefile (xex1tool -b) and matching the callee-saved register save/restore
# pattern (f14-f31, r13-r31, VMX128 to/from the jmp_buf in r3/r7).
# setjmp:  0x82581D20  saves regs to jmp_buf (r3), returns 0.
# longjmp: 0x82581A00  restores regs from jmp_buf (r3), returns val (or 1).
# Codegen special-cases calls to these into ppc_setjmp/ppc_longjmp; without
# them the non-local jump is mis-lifted as an ordinary call (crashes at runtime).
setjmp_address  = 0x82581D20
longjmp_address = 0x82581A00
